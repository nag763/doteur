name: "doteur"
version: "0.1.2"
author: LABEYE Loïc
about: Convert .sql files to .dot files
args:
        - FILENAME:
                help: File of the sql name
                required: true
                index: 1
        - output:
                help: The output filename
                short: o
                takes_value: true
        - include:
                help: include only the given tables
                short: i
                takes_value: true
                multiple: true
        - exclude:
                help: exclude the given tables
                short: x
                takes_value: true
                multiple: true
                conflicts_with:
                        - include
